---
layout: layout
---

<style>
    .paging-prev {
        float: left;
    }

    .paging-next {
        float: right;
    }
</style>
<!-- Post Content -->
<article>
    <div class="row">

        <!-- Post Container -->
        <div class="col-lg-8 col-md-8 col-sm-12 col-xs-12 post-container">
            <h1> <%- page.title %> </h1>
            <%- page.content %>
            <hr>
            <div>
                <p>
                    <span>标签: </span>
                    <% page.tags.each(function(tag) { %>
                    <code key="<%- tag.name%>" class="post-label"><%- tag.name%></code>
                    <% }) %>
                </p>
                <p>本站使用 <a href="https://creativecommons.org/licenses/by/4.0/deed.zh">「署名 4.0 国际」</a> 创作共享协议，转载请注明出处</p>
            </div>

            <hr />
            <p>
                <% if (page.prev){ %>
                <a class="paging-prev" href="<%- page.prev.path %>">« <%- page.prev.title %></a>
                <% } %>
                <% if (page.next){ %>
                <a class="paging-next" href="<%- page.next.path %>"><%- page.next.title %> »</a>
                <% } %>
            </p>
            <%- partial('_widget/comments') %>    
        </div>
        <!-- Tabe of Content -->
        <%- partial('_partial/toc') %>
    </div>
</article>